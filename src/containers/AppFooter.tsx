import React from 'react'
import { Layout } from 'antd'

const { Footer } = Layout

const AppFooter: React.FC<any> = () => {
  return <Footer className='footer'>用户微服务</Footer>
}

export default AppFooter
